use target="_main" tag inside the anchor tag to open the link in new page

to maintain aspect retio of image take only one attribute inside img tage either height or width

span tag is an example of inline element

div and p tag is an example of block element

div and span both are a container

&copy for copyright sign

if any elements has a position absolute then its top, bottom, etc properties will follow its first non static parent. 